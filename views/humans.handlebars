<div id="main">
    <div>
        <h1>List of all persons</h1>
        {{#each listHumans}}
            <div>
                <a href="/humans/{{id}}">- {{id}}: {{name}}</a>
            </div>
        {{/each}}
    </div>
</div>
